---
phase: 01-usable-end-to-end-scanner
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - config/mimir.toml
  - internal/detect/engine.go
  - internal/detect/engine_test.go
  - testdata/fixtures/known-secrets.txt
autonomous: true
requirements:
  - DET-01
  - DET-02
  - DET-03
  - DET-04

must_haves:
  truths:
    - "Scanner detects all v1 signature rules: AWS, GCP, GitHub (5 token types), GitLab (3), Slack (3), Stripe, PEM private key, JWT"
    - "Generic entropy detector fires on a secret-context variable assignment with high-entropy value and is silent on low-entropy or non-secret context (D-10)"
    - "Generic/entropy findings carry rule ID generic-api-key with IsHeuristic=true (D-11)"
    - "Connection-string rule extracts the password capture group (group 3) via SecretGroup=3 in mimir.toml and redacts only that group"
    - "Aho-Corasick trie contains all keywords from all v1 rules; only candidate rules run per line"
    - "Scanner finds no false positives on testdata/clean/ even with full ruleset loaded"
  artifacts:
    - path: "config/mimir.toml"
      provides: "Full v1 ruleset (~15-25 rules) including all DET-01 signature rules, generic-api-key, connection-string"
      contains: "id = \"aws-access-token\""
    - path: "internal/detect/engine.go"
      provides: "Updated Engine supporting all v1 rules; --no-entropy bypass via cfg.NoEntropy (field defined in Plan 01-01 Task 2)"
    - path: "testdata/fixtures/known-secrets.txt"
      provides: "Synthetic tokens matching all v1 rules for true-positive test coverage"
  key_links:
    - from: "config/mimir.toml"
      to: "internal/detect/engine.go"
      via: "all keywords from TOML rules populate the Aho-Corasick trie at NewEngine() time"
      pattern: "NewTrieBuilder|AddStrings"
    - from: "config/mimir.toml"
      to: "internal/detect/engine.go"
      via: "connection-string rule uses secret_group=3; engine's SecretGroup extraction handles it — no separate connstr.go needed"
      pattern: "SecretGroup.*3|secret_group"
---

<objective>
This plan delivers the full v1 detection engine on top of the Walking Skeleton.
After this plan, `mimir scan` detects all 15-25 signature rule types (DET-01),
the keyword-gated entropy generic detector (DET-02), and connection-string
credentials (DET-03), with the Aho-Corasick trie covering all rule keywords
(DET-04).

Purpose: Thicken the skeleton from one rule to the full v1 ruleset. This is the
vertical slice that makes Mimir useful for real repos.

Output: Full config/mimir.toml with all v1 rules (including connection-string
with secret_group=3), engine updated to support rulesForKeywords lookup and
global allowlist check. No new files — connection-string behavior is entirely
handled by the engine's existing SecretGroup extraction path driven by the TOML
rule definition (single source of truth).

File ownership note: internal/config/config.go is owned exclusively by Plan
01-01 (skeleton) and Plan 01-03 (full LoadConfig rewrite). This plan does NOT
touch internal/config/config.go. The NoEntropy bool field is already present
from Plan 01-01 Task 2; this plan's engine reads it via the *config.Config
passed to NewEngine().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-usable-end-to-end-scanner/01-CONTEXT.md
@.planning/phases/01-usable-end-to-end-scanner/01-RESEARCH.md
@.planning/phases/01-usable-end-to-end-scanner/01-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Contracts established by Plan 01-01 (Task 1 + 2). Executor reads these before touching anything. -->

internal/config/config.go (from Plan 01-01 Task 2 — DO NOT MODIFY in this plan):
  type Rule struct {
    ID             string
    Description    string
    Regex          string
    CompiledRegex  *regexp.Regexp
    Entropy        float64
    Keywords       []string
    SecretGroup    int
    IsHeuristic    bool
    Allowlists     []Allowlist
  }
  type Config struct {
    Rules            []Rule
    GlobalAllowlists []Allowlist
    MaxFileSizeMB    int
    NoEntropy        bool   // set from --no-entropy flag in cmd/scan.go (Plan 01-03)
    Verbose          bool
  }

internal/detect/engine.go (from Plan 01-01 Task 2):
  type Engine struct { trie *ahocorasick.Trie; rules []config.Rule; cfg *config.Config; globalAllowlists []config.Allowlist }
  func NewEngine(cfg *config.Config) *Engine
  func (e *Engine) ScanLine(line, filePath string, lineNum int) []finding.Finding

The engine receives NoEntropy via the *config.Config passed to NewEngine() in cmd/scan.go.
Plan 02 does not modify Config struct. The entropy skip in ScanLine already checks cfg.NoEntropy.
The engine ScanLine must remain goroutine-safe (read-only after construction).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full v1 TOML ruleset + fixture tokens for all rules</name>
  <files>
    config/mimir.toml,
    testdata/fixtures/known-secrets.txt
  </files>
  <read_first>
    .planning/phases/01-usable-end-to-end-scanner/01-RESEARCH.md — §V1 Ruleset Patterns (complete patterns for all DET-01 and DET-02 rules), §Connection-String Rule, §Config/Ruleset Format (TOML schema, global allowlists, extend model), §Common Pitfalls (Pitfall 3: keyword case; A3: generic-api-key TOML escaping)
    .planning/phases/01-usable-end-to-end-scanner/01-CONTEXT.md — D-07 (focused ruleset, 15-25 rules), D-08 (connection-string one generic URI rule), D-09 (extend model), D-10 (entropy defaults), D-11 (generic rule IDs)
    config/mimir.toml (currently has only aws-access-token; must be expanded)
    ./CLAUDE.md — go-toml/v2 multiline literal strings for complex regex
  </read_first>
  <behavior>
    - Test (go test ./internal/config/ -run TestRulesetParse): config.LoadDefault() returns non-nil Config with >= 15 rules, no error
    - Test: Each rule in the loaded config has a non-nil CompiledRegex (RE2 validated at load)
    - Test: Every rule has at least one keyword
    - Test: The generic-api-key rule has IsHeuristic=true
    - Test: The connection-string rule has SecretGroup=3
  </behavior>
  <action>
    Expand config/mimir.toml with the complete v1 ruleset. Use the exact patterns from RESEARCH.md §V1 Ruleset Patterns for each rule. TOML formatting rules:
      - Use multiline literal strings ('''...''') for any regex containing backslashes, quotes, or complex character classes (RESEARCH.md A3 warning).
      - Keywords MUST be lowercase strings (Pitfall 3).
      - For each rule: id, description, regex, entropy (if > 0), keywords, and [[rules.allowlists]] if needed.

    Connection-string rule: define in TOML with secret_group=3. This is the SINGLE canonical source
    for the connection-string regex. The engine's existing SecretGroup extraction handles group 3
    extraction uniformly — no separate connstr.go file is needed or created. Pattern (from RESEARCH.md
    §Connection-String Rule): matches scheme://user:password@host forms; group 3 = password span.

    Rules to include (in order):
    1. aws-access-token (already present; verify pattern matches RESEARCH.md exactly)
    2. gcp-api-key (AIza prefix, 39 chars, entropy 4.0, keyword "aiza")
    3. github-pat (ghp_ prefix, entropy 3.0)
    4. github-oauth (gho_ prefix, entropy 3.0)
    5. github-app-token (ghu_/ghs_ prefix, entropy 3.0)
    6. github-refresh-token (ghr_ prefix, entropy 3.0)
    7. github-fine-grained-pat (github_pat_ prefix, entropy 3.0)
    8. gitlab-pat (glpat- prefix, entropy 3.0)
    9. gitlab-cicd-job-token (glcbt- prefix, entropy 3.0)
    10. gitlab-runner-token (glrt- prefix, entropy 3.0)
    11. slack-bot-token (xoxb- prefix, entropy 3.0)
    12. slack-user-token (xoxp-/xoxe- prefix, entropy 2.0)
    13. slack-webhook (hooks.slack.com keyword)
    14. stripe-access-token (sk_test/sk_live/sk_prod/rk_ prefixes, entropy 2.0)
    15. private-key (-----BEGIN ... PRIVATE KEY prefix)
    16. jwt (ey header + payload pattern, entropy 3.0)
    17. generic-api-key (IsHeuristic rule; entropy 3.5; keywords: access/api/auth/key/credential/creds/passwd/password/secret/token; allowlist for pure-alpha strings)
    18. connection-string (secret_group=3; keyword "://"; regex from RESEARCH.md §Connection-String Rule)

    Global allowlists (2 blocks):
      Block 1: common placeholder patterns ('^(?i:true|false|null|undefined|none|empty)$', '^\$\{[A-Za-z_]\w*\}$', '^\$[A-Z_][A-Z0-9_]*$', '^\{\{[^}]+\}\}$')
      Block 2: noisy path patterns ('go\.(?:mod|sum)$', '(?:^|/)vendor/', '(?:^|/)\.git/')

    Extend block (at end of file):
      [extend]
      use_default = true
      disabled_rules = []

    Expand testdata/fixtures/known-secrets.txt to include synthetic tokens for ALL rules:
      - aws-access-token: AKIAFAKEKEY12345678 (already exists; verify format)
      - gcp-api-key: AIzaFakeKey1234567890123456789012345 (39 chars starting AIza)
      - github-pat: ghp_FakeGitHubToken123456789012345678  (ghp_ + 36 alphanum)
      - github-oauth: gho_FakeGitHubOAuth123456789012345678
      - github-app-token: ghu_FakeGitHubApp1234567890123456789
      - gitlab-pat: glpat-FakegitLabTok12345678  (glpat- + 20 word chars)
      - slack-bot-token: xoxb-1234567890-1234567890-FakeBotToken  (numeric segments)
      - stripe-access-token: sk_test_FakeStripeKey1234567890
      - private-key: -----BEGIN RSA PRIVATE KEY-----
      - jwt: eyFakeJWTHeader1234567.eyFakeJWTPayload12345678.FakeJWTSignature123456789
      - generic-api-key: api_key = "FakeHighEntropyKey123456789XYZ"  (must have entropy > 3.5)
      - connection-string: postgres://user:FakeSecretPass123@localhost:5432/mydb
      Each token MUST be clearly fake (obvious prefix like "Fake", repeated chars, or not real credentials). Add a file header comment: "# Test fixtures: synthetic tokens for unit tests. NOT real credentials."
  </action>
  <verify>
    <automated>go test ./internal/config/ -run TestRulesetParse -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/config/ -run TestRulesetParse exits 0 with PASS
    - config.LoadDefault() returns Config with >= 15 rules and nil error
    - Every rule in the loaded config has non-nil CompiledRegex (RE2-validated)
    - Rule with id "generic-api-key" has IsHeuristic==true
    - Rule with id "connection-string" has SecretGroup==3
    - All keywords in all rules are lowercase strings (case-sensitive trie requirement)
    - testdata/fixtures/known-secrets.txt contains lines matching each v1 rule type
    - go test ./internal/config/ -run TestRulesetParse -v shows no "invalid regex" errors
    - go build ./... exits 0 (no compile errors after mimir.toml expansion)
  </acceptance_criteria>
  <done>Full v1 TOML ruleset with all 15-25 rules parses cleanly via go-toml/v2 with RE2-validated patterns. Connection-string rule defined in TOML with secret_group=3 (single canonical regex source). Fixture file covers all rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine updates — rulesForKeywords lookup, global allowlists, all-rules tests</name>
  <files>
    internal/detect/engine.go,
    internal/detect/engine_test.go
  </files>
  <read_first>
    .planning/phases/01-usable-end-to-end-scanner/01-RESEARCH.md — §Connection-String Rule (capture groups, tested URIs, @-in-password limitation), §Aho-Corasick Pre-filter (MatchString for all matches, rulesForKeywords), §Full Pipeline: Scan One Line (secret_group handling), §Common Pitfalls (Pitfall 7: @ in password)
    .planning/phases/01-usable-end-to-end-scanner/01-CONTEXT.md — D-10 (--no-entropy; engine reads cfg.NoEntropy), D-11 (IsHeuristic marker for generic-* rules)
    internal/detect/engine.go (Plan 01-01 Task 2 — read current state before modifying)
    internal/config/config.go (Plan 01-01 Task 2 — read Config struct; DO NOT MODIFY this file)
    ./CLAUDE.md — stdlib regexp only
  </read_first>
  <behavior>
    - Test: Engine scans "postgres://user:FakeSecretPass123@localhost:5432/mydb" returns 1 Finding with RuleID "connection-string" and Secret containing redacted password (not "FakeSecretPass123")
    - Test: Engine scans "redis://:sometoken@redis.host.io:6379" returns finding with RuleID "connection-string" (password-only form, empty user)
    - Test: Engine scans "https://example.com/page" returns nil (no user:password@ form)
    - Test: Engine with cfg.NoEntropy=true does NOT apply entropy threshold — a low-entropy match that normally fails entropy check IS returned
    - Test: Engine with cfg.NoEntropy=false (default) skips match where entropy < rule.Entropy threshold
    - Test: Engine.ScanLine for all v1 rules: each fixture from testdata/fixtures/known-secrets.txt triggers its corresponding rule
    - Test: Engine.ScanLine on testdata/clean/no-secrets.go content returns nil for all lines (FP regression)
  </behavior>
  <action>
    internal/detect/engine.go — update (do NOT modify internal/config/config.go; NoEntropy bool already present there from Plan 01-01 Task 2):

    1. rulesForKeywords(matches []*ahocorasick.Match) []config.Rule:
       Build a keyword-to-rules map at NewEngine time (map[string][]int where value is rule indices).
       For each match in matches, collect rules whose Keywords contain match.MatchString().
       Deduplicate rule indices before running regexes.
       Store this keyword-to-rule-index map on the Engine struct at construction time.

    2. Global allowlist check: after regex match, check engine.globalAllowlists (populated from
       cfg.GlobalAllowlists in NewEngine). If the rawSecret matches any global allowlist regex { continue }.
       This is already partially present from Plan 01-01 Task 2; verify it covers GlobalAllowlists.

    3. Verify entropy skip uses cfg.NoEntropy correctly (already coded in Plan 01-01 Task 2):
       if rule.Entropy > 0 && !engine.cfg.NoEntropy && shannonEntropy(rawSecret) <= rule.Entropy { continue }
       When NoEntropy=true, the entropy threshold is skipped entirely.

    4. SecretGroup extraction: for rules with SecretGroup > 0, use submatches[SecretGroup] as rawSecret.
       Default (SecretGroup == 0) uses submatches[1]. This handles connection-string (group 3) and all
       other rules uniformly. No special-case code for connection-string is needed here.

    internal/detect/engine_test.go — add/expand tests:
      TestConnStr: connection-string detection with various URI forms (postgres, redis, no-creds).
      TestNoEntropy: with and without NoEntropy flag.
      TestAllRules: for each rule ID in the default config, find its corresponding fixture line in testdata/fixtures/known-secrets.txt, confirm ScanLine returns a Finding with that rule ID.
      TestCleanNoFP: scan every line in testdata/clean/no-secrets.go with full ruleset — assert zero findings.
  </action>
  <verify>
    <automated>go test ./internal/detect/ -run 'TestConnStr|TestNoEntropy|TestAllRules|TestCleanNoFP' -v -count=1 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/detect/ exits 0 with all PASS
    - TestConnStr: "postgres://user:FakeSecretPass123@localhost:5432/mydb" → Finding with RuleID "connection-string"; Finding.Secret contains redacted password (not raw "FakeSecretPass123")
    - TestConnStr: "redis://:sometoken@redis.host.io:6379" → Finding with RuleID "connection-string"
    - TestConnStr: "https://example.com/page" (no user:pass) → 0 findings
    - TestNoEntropy: when cfg.NoEntropy=true, a match failing entropy threshold IS returned; when cfg.NoEntropy=false, it is NOT returned
    - TestAllRules: every rule ID in the default config produces >= 1 finding from its corresponding fixture line
    - TestCleanNoFP: testdata/clean/no-secrets.go produces 0 findings with full ruleset (no false positives)
    - go test -race ./internal/detect/ exits 0 (no data races)
    - go test ./internal/scanner/ exits 0 after engine update (scanner tests still pass)
    - The binary still builds: go build ./... exits 0
    - /tmp/mimir-test scan testdata/fixtures/ now finds >= 5 rule types (not just aws-access-token)
    - internal/config/config.go is NOT modified by this plan (verify with git diff --name-only)
  </acceptance_criteria>
  <done>Full v1 detection engine active: all 15-25 rules in the Aho-Corasick trie, rulesForKeywords map eliminates redundant regex runs, entropy checks per-rule (skippable via cfg.NoEntropy), connection-string passwords extracted via SecretGroup=3 (TOML as single regex source), global allowlists applied, no false positives on clean fixtures. No file overlap with Plan 01-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| config/mimir.toml regex patterns → regexp.Compile | Embedded ruleset patterns are trusted (developer-authored); user custom rules (Plan 03) are untrusted and validated separately |
| File line content → ScanLine | Attacker-controlled repository content enters the regex/entropy pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Denial of Service | `internal/detect/engine.go` — all 15-25 rules, Aho-Corasick + RE2 | mitigate | All patterns are RE2 (linear-time). Aho-Corasick gate prevents regex execution on unmatched lines. max-file-size cap (10 MB) bounds per-file work. |
| T-02-02 | Information Disclosure | Connection-string `secret_group=3` extraction | mitigate | Password extracted via capture group; passed immediately to finding.New() which redacts at boundary. The raw password group string is never stored or logged. Single regex source in config/mimir.toml eliminates divergence risk. |
| T-02-03 | Information Disclosure | Generic entropy regex — broad match before entropy gate | mitigate | Even if entropy gate passes, finding.New() is the only consumer of rawSecret; redaction is enforced at the constructor boundary. |
| T-02-04 | Tampering | TOML multiline literal strings with complex regex | mitigate | All patterns explicitly RE2-validated by config.LoadDefault() via regexp.Compile. Invalid pattern → LoadDefault returns error → os.Exit(2) in cmd/scan.go. |
| T-02-05 | Denial of Service | generic-api-key regex is broad (many keywords) | accept | Entropy gate (3.5) and allowlist (pure-alpha filter) provide defense-in-depth against false triggers. Keyword pre-filter still applies — line must contain one of the 10 generic keywords. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in Plan 02 — uses same locked set from Plan 01-01. go mod verify confirms checksums. |
</threat_model>

<verification>
Full plan-02 verification:

1. `go test ./internal/config/ -run TestRulesetParse -v` — exits 0, >= 15 rules loaded
2. `go test ./internal/detect/ -v -count=1` — exits 0, all rule tests pass
3. `go test -race ./internal/detect/ ./internal/config/` — exits 0, no races
4. `git diff --name-only HEAD` — internal/config/config.go NOT in the diff (file ownership respected)
5. `go build -o /tmp/mimir-test ./cmd/mimir && /tmp/mimir-test scan testdata/fixtures/` — exits 1, multiple rule IDs visible in output
6. `grep -c "aws-access-token\|github-pat\|stripe-access-token\|private-key\|connection-string" <(cd /path/to/mimir && ./mimir-test scan testdata/fixtures/)` — count >= 5
7. `/tmp/mimir-test scan testdata/clean/` — exits 0 (full ruleset produces no false positives on clean files)
</verification>

<success_criteria>
- [ ] config.LoadDefault() returns Config with >= 15 rules, nil error
- [ ] All 15-25 rules have RE2-validated CompiledRegex (no nil entries)
- [ ] Each rule's fixture token in testdata/fixtures/ triggers the correct rule ID
- [ ] Connection-string rule detects postgres://, redis://, mongodb:// URIs with embedded credentials (via secret_group=3 in TOML — single canonical regex source)
- [ ] --no-entropy bypass works: low-entropy match passes when cfg.NoEntropy=true
- [ ] Full ruleset produces 0 false positives on testdata/clean/no-secrets.go
- [ ] internal/config/config.go is NOT modified (owned by Plans 01-01 and 01-03)
- [ ] All tests pass: `go test ./... -count=1` exits 0
- [ ] Race-free: `go test -race ./...` exits 0
- [ ] Binary with full ruleset still exits correct codes
</success_criteria>

<output>
Create `.planning/phases/01-usable-end-to-end-scanner/01-02-SUMMARY.md` when done.
</output>
