# Forensic Report

**Generated:** 2026-05-28T21:30:13Z
**Problem:** SDK resolution failure — the `/gsd-plan-phase 2` workflow's `gsd-sdk` resolution preamble selected an incompatible binary, so `$GSD_SDK query init.plan-phase 2` failed.

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-05-28 22:24:03 +0100 — "docs(state): record phase 2 context session"
- **Commits (last 15):** clean, linear Phase 1 → Phase 2 progression; no rapid re-commit churn
- **Uncommitted changes:** yes — `M .planning/config.json`, `?? .claude/` (untracked, fresh local GSD install)
- **Active worktrees:** 1 (main only — no orphaned worktrees)

### Planning State
- **Current milestone:** v1.0
- **Current phase:** 2 — False-Positive Control — status `Pending` (context gathered, ready to plan)
- **Last session:** "Phase 2 context gathered — ready to plan"
- **Blockers:** none in STATE.md (the failure is tooling, not project state)

### Artifact Completeness
| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|-------------|
| 01 — usable-end-to-end-scanner | ✅ (3) | ✅ | ✅ | ✅ (3) | ✅ |
| 02 — false-positive-control | ❌ | ✅ | ❌ | ❌ | ❌ |

Phase 2 having only CONTEXT.md is expected — planning had not run yet (it is what was being attempted).

## Anomalies Detected

### SDK Path Resolution Omits Install Prefix — Confidence: HIGH
**Evidence:** The resolution preamble shared by **73 workflow files** computes:
```
GSD_TOOLS="${RUNTIME_DIR:-$(git rev-parse --show-toplevel)}/get-shit-done/bin/gsd-tools.cjs"
```
- `RUNTIME_DIR` is unset in this environment (it is only ever assigned *inside* `update.md`).
- Fallback therefore resolves to `<repo>/get-shit-done/bin/gsd-tools.cjs` → **does not exist**.
- The actual install is at `<repo>/.claude/get-shit-done/bin/gsd-tools.cjs` → **exists** (verified).

The `[ -f "$GSD_TOOLS" ]` test fails, so the local bundled tool is never selected.

**Interpretation:** The fallback path is missing the `.claude/` (local) or `~/.claude/` (global) config-dir prefix. It assumes `get-shit-done/` sits at the repo root, but Claude Code installs it under `.claude/`.

### Global `gsd-sdk` Is a Name-Colliding Incompatible Binary — Confidence: HIGH
**Evidence:** With the path test failing, resolution falls through to `elif command -v gsd-sdk`:
- `which gsd-sdk` → `/usr/local/bin/gsd-sdk`
- `gsd-sdk --version` → `gsd-sdk v0.1.0`
- `gsd-sdk query state.json` → `Error: Expected "gsd-sdk run <prompt>", "gsd-sdk auto", or "gsd-sdk init"`

The installed global `gsd-sdk v0.1.0` is the **autonomous milestone runner** (`run` / `auto` / `init` only). It is a *different program* from the query-helper the workflows expect — a classic PATH name collision (the same failure class CLAUDE.md already warns about for `rtk`).

**Interpretation:** Both resolution branches lead to a tool that cannot answer `query`. The first branch can't find the right file; the second finds the wrong program.

### Workflow `query` Grammar Matches Neither Installed Binary — Confidence: HIGH
**Evidence:** Workflows issue ~250+ calls of the form `$GSD_SDK query <namespace>.<verb>` (101× `query config-get`, 62× `query commit`, 22× `query init.phase-op`, 18× `query roadmap.get-phase`, …). Tested against both candidates:
- `node .claude/.../gsd-tools.cjs query state.json` → `Error: Unknown command: query`
- `gsd-sdk query state.json` → usage error (no `query` verb)

Both the local bundled tool **and** the global binary are reported as **v1.1.0 / v0.1.0** respectively, yet *neither* accepts the literal `query <verb>` grammar. The bundled `gsd-tools.cjs` v1.1.0 uses **bare subcommands** instead: `init plan-phase 2`, `find-phase 2`, `config-get <key>`, `roadmap get-phase <n>` (all verified working).

**Interpretation:** Even after the path bug is fixed and the correct local tool is selected, running workflow commands verbatim still fails, because the workflow templates encode a `query`-dispatch abstraction the shipped `gsd-tools.cjs` v1.1.0 does not implement. The workflow docs and the bundled CLI are out of sync on command grammar within the same nominal version.

## Root Cause Hypothesis

The failure is purely in GSD tooling resolution, not in project state. Two independent defects compound: (1) the 73-workflow resolution preamble builds the bundled-tool path without the `.claude/` install prefix, so it never finds the local `gsd-tools.cjs` and falls through to a PATH `gsd-sdk` that is an unrelated v0.1.0 program; and (2) the workflows' `query <namespace>.<verb>` grammar is not implemented by the shipped `gsd-tools.cjs` v1.1.0, which expects bare `<namespace> <verb>` subcommands. The recent untracked `.claude/` install (today) most likely introduced or exposed this skew.

## Recommended Actions

1. **Immediate unblock (this session):** call the bundled tool directly, dropping `query` and converting dots to spaces. This is exactly how planning was driven manually and it works:
   ```bash
   GSD="node ./.claude/get-shit-done/bin/gsd-tools.cjs"
   $GSD init plan-phase 2        # not: query init.plan-phase
   $GSD find-phase 2
   $GSD roadmap get-phase 2      # not: query roadmap.get-phase
   ```
2. **Session-scoped path fix:** export `RUNTIME_DIR` so the preamble's first branch wins:
   ```bash
   export RUNTIME_DIR="$(git rev-parse --show-toplevel)/.claude"
   ```
   (Selects the local tool, but does NOT fix the `query`-grammar mismatch — pair with action 1.)
3. **Durable fix — realign tool and workflows:** run `/gsd-update` (or reinstall via `npx get-shit-done-cc@latest --claude --local`) to get a `gsd-tools.cjs` whose grammar matches the workflow templates, OR file upstream: within v1.1.0 the bundled CLI lacks the `query` dispatcher the workflows assume.
4. **PATH hygiene:** the global `/usr/local/bin/gsd-sdk v0.1.0` (autonomous runner) shadows the name the query workflows expect. Either remove/rename it or ensure the local bundled-tool branch always resolves first, to avoid silently selecting the wrong program.
5. **Resume work** after applying action 1 (and optionally 2): re-run `/gsd-plan-phase 2` — Phase 2 context is complete and ready to plan.

---

*Report generated by `/gsd-forensics`. Paths shown relative to repo root for portability.*
