# Synthetic test fixtures for Mimir scanner unit tests.
# These tokens match the regex patterns but are NOT real credentials —
# they use repeated characters / obvious patterns that have low entropy,
# or are explicitly labelled as fake.

# aws-access-token rule (AKIA prefix + 16 uppercase alphanumeric)
aws_access_key_id = AKIAFAKEKEY12345678
aws_secret_access_key = AKIAXXXXXXXXXXX3456

# Stub placeholders for rules added in Plan 02 (commented out to avoid
# triggering external scanners on obviously fake values):
# github-pat: ghp_ prefix + 36 alphanum chars (Plan 02 adds this rule)
# stripe-access-token: sk_test_ prefix (Plan 02 adds this rule)
# private-key header: -----BEGIN RSA PRIVATE KEY----- (Plan 02 adds this rule)
