---
phase: 04-opt-in-live-verification-aws-github
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - cmd/mimir/scan.go
  - cmd/mimir/scan_verify_test.go
  - cmd/mimir/hook_test.go
  - internal/output/human.go
  - internal/output/human_test.go
  - internal/output/json_test.go
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03]
must_haves:
  truths:
    - "User can pass --verify (off by default); without it, no network calls and JSON byte-identical to OUT-02"
    - "Under --verify, verify.Run runs on the post-suppression newFindings set (before output), consuming the Plan 01 raw map"
    - "--verify never changes the exit code (label-only) — new findings still exit 1, errors exit 2, clean exits 0"
    - "Human output shows a colored ACTIVE/INACTIVE/UNKNOWN tag per verified finding plus a one-line tally; JSON carries the nested verification object only when set"
    - "The installed pre-commit hook template never contains --verify (hook stays offline)"
  artifacts:
    - path: "cmd/mimir/scan.go"
      provides: "--verify flag registration + verify.Run invocation on newFindings"
      contains: "verify.Run"
    - path: "internal/output/human.go"
      provides: "colored verification tag + Verified tally"
      contains: "Verification"
  key_links:
    - from: "cmd/mimir/scan.go runScan"
      to: "verify.Run(cmd.Context(), newFindings, raw)"
      via: "post-suppression invocation guarded by the --verify flag"
      pattern: "verify\\.Run"
    - from: "cmd/mimir/scan.go"
      to: "raw map from Scan/ScanHistory/ScanStaged"
      via: "the fingerprint→raw return threaded from Plan 01"
      pattern: "raw"
---

<objective>
Wire `--verify` end-to-end: register the off-by-default flag, capture the raw map from each scan
source (Plan 01), invoke `verify.Run` (Plan 02) on the post-suppression `newFindings` set before
output, and render the result — a colored ACTIVE/INACTIVE/UNKNOWN tag per verified finding plus a
one-line `Verified: N active, M inactive, K unknown` tally in human output, and the nested
`verification` object in JSON (omitted when absent). `--verify` is strictly label-only: it never
changes the exit-code contract. A dedicated test confirms the pre-commit hook template stays offline.

Purpose: Deliver the user-facing VERIFY-01 capability (opt-in, never in the hook, no network without
the flag) and the VERIFY-02/03 status rendering, completing the phase goal: turning "is this real?"
into an actionable active/inactive/unknown label without touching the detection engine or exit codes.
Output: `--verify` flag + runScan invocation; human/JSON rendering; off-by-default no-network test;
OUT-02 omit-default test; dedicated hook-offline test.
</objective>

<execution_context>
@/home/oliverh/repos/github/MatrixMagician/Mimir/.claude/get-shit-done/workflows/execute-plan.md
@/home/oliverh/repos/github/MatrixMagician/Mimir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-opt-in-live-verification-aws-github/04-CONTEXT.md
@.planning/phases/04-opt-in-live-verification-aws-github/04-RESEARCH.md
@.planning/phases/04-opt-in-live-verification-aws-github/04-PATTERNS.md
@.planning/phases/04-opt-in-live-verification-aws-github/04-VALIDATION.md
@cmd/mimir/scan.go
@internal/output/human.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register --verify and invoke verify.Run on the post-suppression set</name>
  <files>cmd/mimir/scan.go, cmd/mimir/scan_verify_test.go</files>
  <read_first>
    - cmd/mimir/scan.go (init() flag block lines 25-42 — the bool-flag idiom for --git/--staged at 35-36; the source dispatch switch lines 108-117; the newFindings post-suppression loop lines 148-155; the exit-code contract lines 191-197)
    - internal/verify/verify.go (the Run(ctx, findings, rawByFP) signature from Plan 02)
    - internal/scanner/scanner.go + internal/gitscan/gitscan.go (the new raw-map-before-Stats return signatures from Plan 01)
    - .planning/phases/04-opt-in-live-verification-aws-github/04-RESEARCH.md (Pitfall 6 — hook offline; the invocation slot in the architecture diagram; Anti-Pattern: verify only newFindings)
  </read_first>
  <behavior>
    - `mimir scan <dir>` WITHOUT --verify makes zero network calls and produces JSON byte-identical to the pre-Phase-4 OUT-02 schema (no "verification" key).
    - `mimir scan --verify <dir>` invokes verify.Run on newFindings (the post-suppression reportable set) before output; findings of unverifiable rule types stay unlabeled.
    - --verify does NOT alter the exit code: a repo with one new finding exits 1 regardless of active/inactive/unknown; a clean repo exits 0; a git error still exits 2.
    - --verify is accepted alongside --staged for manual runs (no mutual-exclusion branch added).
  </behavior>
  <action>
    In `init()` add `scanCmd.Flags().Bool("verify", false, "Live-verify AWS/GitHub findings (off by default; makes network calls; never used by the pre-commit hook)")` next to the --git/--staged flags. In `runScan`, capture the new raw map from each source: change the three dispatch calls (lines 112/114/116) to receive the fingerprint→raw map (Plan 01 added it BEFORE Stats): `findings, raw, stats, err = gitscan.ScanHistory(...)` / `ScanStaged(...)` / `s.Scan(...)`. Declare `var raw map[string]string` alongside `var findings`/`var stats`. Read `verify, _ := cmd.Flags().GetBool("verify")` (name the local `doVerify` to avoid shadowing the package import). Immediately AFTER the newFindings loop (after line 155) and BEFORE the baseline-out / output stages, insert: `if doVerify { verify.Run(cmd.Context(), newFindings, raw) }` — run ONLY on newFindings (RESEARCH Anti-Pattern: never the full set). Add the `internal/verify` import. Do NOT add any fail-loud branch for --verify and do NOT touch the exit-code contract (lines 191-197) — it stays label-only. Create `cmd/mimir/scan_verify_test.go` with `TestScanNoVerifyNoNetwork`: run a scan WITHOUT --verify over a fixture containing the fixture secret (use the black-box os/exec TestMain harness already used by cmd tests), assert exit behavior is unchanged and stdout JSON contains no "verification" substring (proving no network path ran). Use a fixture secret that matches a rule but is not auto-allowlisted (AKIAFAKEKEYABCDE2345).
  </action>
  <verify>
    <automated>/usr/local/go/bin/go build ./... && /usr/local/go/bin/go test ./cmd/... -run TestScanNoVerifyNoNetwork -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/mimir/scan.go` registers a `verify` bool flag defaulting to false.
    - Source: the three dispatch calls receive the `raw` map (raw before Stats); `runScan` declares `var raw map[string]string`.
    - Source: `if doVerify { verify.Run(cmd.Context(), newFindings, raw) }` appears AFTER the newFindings loop and BEFORE output; grep confirms verify.Run is passed `newFindings`, not `findings`.
    - Source: no new mutual-exclusion / fail-loud branch references `verify`; the exit-code block (lines ~191-197) is unchanged.
    - Behavior: `go build ./...` succeeds (the raw return from Plan 01 is now consumed, not discarded).
    - Behavior: `go test ./cmd/... -run TestScanNoVerifyNoNetwork -count=1` passes (no "verification" in non-verify JSON; exit codes unchanged).
  </acceptance_criteria>
  <done>--verify flag registered off-by-default; raw map captured and passed to verify.Run on newFindings before output; exit-code contract untouched; no-verify-no-network test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render verification tag + tally in human output; confirm JSON omit-default</name>
  <files>internal/output/human.go, internal/output/human_test.go, internal/output/json_test.go</files>
  <read_first>
    - internal/output/human.go (color style vars lines 17-22; the CommitSHA conditional-tag render lines 64-70 — the analog to mirror with an `if f.Verification != nil` branch; the suppression-count tally loop lines 113-117 — the analog for the Verified tally; sanitizeForTTY lines 146-177)
    - internal/output/output_test.go (test conventions: package X_test, color.NoColor=true for deterministic assertions, table t.Run subtests)
    - internal/finding/finding.go (the Verification type from Plan 01 — Status/Provider enums)
  </read_first>
  <behavior>
    - A finding with Verification.Status=="active" renders an ACTIVE tag (red+bold when color enabled); "inactive" renders INACTIVE (dim); "unknown" renders UNKNOWN (yellow). With color.NoColor=true the plain-text tag string (ACTIVE/INACTIVE/UNKNOWN) is present in the row.
    - A finding with nil Verification renders exactly the pre-Phase-4 row (no tag) — OUT-02/human output byte-identical for non-verify scans.
    - When any finding carries Verification, a one-line tally `Verified: N active, M inactive, K unknown` is printed in the summary (counts reflect the rendered findings). No tally line when no finding is verified.
    - JSON: a finding with nil Verification omits the "verification" key; a finding with Verification set emits `"verification":{"status":...,"provider":...}` (WriteJSON needs no change beyond the Plan 01 struct — assert it).
  </behavior>
  <action>
    In `internal/output/human.go` add a `verifyActiveStyle = color.New(color.FgRed, color.Bold)` style var (reuse `heuStyle`/yellow for UNKNOWN and an `FgHiBlack` dim style for INACTIVE — mirror the suppressed-section dim at line 91). In the active-finding render block, add an `if f.Verification != nil` branch that appends a colored tag (ACTIVE/INACTIVE/UNKNOWN chosen by f.Verification.Status) to the finding row — mirror the CommitSHA conditional-render discipline (lines 64-70) so the no-Verification path stays byte-identical. The tag is a fixed enum so it needs no sanitizeForTTY, but do NOT print any verifier-sourced free string. Accumulate active/inactive/unknown counts while iterating; after the existing summary line (and the suppression-count loop), if any count > 0 print a single `Verified: N active, M inactive, K unknown` line (mirror the tally loop at 113-117). Add `human_test.go` cases (color.NoColor=true): TestHumanVerificationTag (active/inactive/unknown rows contain the right tag word; a nil-Verification finding row is unchanged) and TestHumanVerifiedTally (the tally line appears with correct counts; absent when none verified). Add `json_test.go` `TestVerifyOmittedByDefault`: marshal a findings slice with nil Verification via WriteJSON and assert.NotContains "verification"; then one with Verification set and assert.Contains the nested object — proving the omitempty pointer behavior end-to-end through WriteJSON.
  </action>
  <verify>
    <automated>/usr/local/go/bin/go test ./internal/output/... -run 'TestHumanVerificationTag|TestHumanVerifiedTally|TestVerifyOmittedByDefault' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `human.go` has an `if f.Verification != nil` branch appending a colored tag and a post-summary `Verified: N active, M inactive, K unknown` line gated on a non-zero count.
    - Source: the nil-Verification render path is byte-identical to the pre-Phase-4 row (no tag, no tally).
    - Behavior: `go test ./internal/output -run TestHumanVerificationTag -count=1` passes (ACTIVE/INACTIVE/UNKNOWN words present per status; nil row unchanged).
    - Behavior: `go test ./internal/output -run TestHumanVerifiedTally -count=1` passes (tally counts correct; absent when none).
    - Behavior: `go test ./internal/output -run TestVerifyOmittedByDefault -count=1` passes (omitempty pointer omits "verification" by default; nested object present when set).
  </acceptance_criteria>
  <done>Human output renders the colored verification tag and the Verified tally with byte-identical non-verify rows; JSON omits verification by default and emits the nested object when set; output tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dedicated hook-offline test (VERIFY-01) + full-suite phase gate</name>
  <files>cmd/mimir/hook_test.go</files>
  <read_first>
    - cmd/mimir/hook_test.go (the existing TestHookInstall asserts `assert.NotContains(t, s, "--verify", ...)` at line 62 — the offline guard; the hookRepo/hookPath helpers; the install invocation)
    - cmd/mimir/hook.go (the hook template — confirm it is NOT modified by this phase; line 28 `exec mimir scan --staged`)
    - .planning/phases/04-opt-in-live-verification-aws-github/04-VALIDATION.md (the hook-offline row references test name TestHookOffline)
  </read_first>
  <behavior>
    - A dedicated `TestHookOffline` installs the hook and asserts the generated hook script string does NOT contain "--verify" — matching the VALIDATION map's named command `go test ./... -run TestHookOffline`.
    - The hook template file (cmd/mimir/hook.go) is unmodified by Phase 4 — no network capability added.
    - The full -race suite passes: all Phase 4 + prior tests green, proving the additive changes broke nothing.
  </behavior>
  <action>
    Add a focused `TestHookOffline` to `cmd/mimir/hook_test.go` that reuses the existing hookRepo/hookPath helpers to install the hook, reads the generated hook script, and asserts.NotContains "--verify" (VERIFY-01 / Pitfall 6) — this gives the VALIDATION map its explicitly named test while leaving the existing in-line assertion in TestHookInstall intact (do NOT remove it). Do NOT modify cmd/mimir/hook.go (the template must stay offline). Then run the full -race suite as the phase gate. Fix any signature drift surfaced by the suite (e.g. a stray pre-Phase-4 test still calling the old Scan/ScanHistory arity) by updating the test call sites to the new raw-map-before-Stats returns — but make NO production behavior change.
  </action>
  <verify>
    <automated>/usr/local/go/bin/go test ./... -run TestHookOffline -count=1 && /usr/local/go/bin/go test -race ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/mimir/hook_test.go` contains `func TestHookOffline` asserting the hook script NotContains "--verify".
    - Source: `cmd/mimir/hook.go` is byte-unchanged by this phase.
    - Behavior: `go test ./... -run TestHookOffline -count=1` passes.
    - Behavior: `go test -race ./... -count=1` is fully green (entire repo).
    - Behavior: `go vet ./...` clean (run as part of the gate).
  </acceptance_criteria>
  <done>Dedicated TestHookOffline green and matching the VALIDATION map; hook template unchanged; full -race suite + go vet green across the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag → network capability | --verify is the ONLY switch that turns on outbound calls; default-off is the safety property |
| pre-commit hook → network | The installed hook must NEVER gain a network call (offline guarantee) |
| verification status → output | Status enums cross into human/JSON rendering; must not leak any verifier free-string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-hook | Tampering (scope creep) | pre-commit hook | mitigate | --verify defaults false; hook template never emits it. Tests: TestHookOffline (dedicated) + the existing in-line assertion in TestHookInstall. (BLOCK-WORTHY: hook going online) |
| T-04-default | Information Disclosure / DoS | default scan path | mitigate | No --verify → verify.Run never called → zero network. Test: TestScanNoVerifyNoNetwork. |
| T-04-schema | Tampering | OUT-02 JSON | mitigate | nil Verification → omitempty omits the key → byte-identical non-verify JSON. Test: TestVerifyOmittedByDefault. |
| T-04-exit | Tampering | exit-code contract | mitigate | --verify is label-only; the exit block is unchanged; new findings still exit 1, errors exit 2. Asserted in TestScanNoVerifyNoNetwork. |
| T-04-tty | Information Disclosure | human output | mitigate | The verification tag is a fixed enum (no verifier free-string printed); all other repo-sourced strings keep their sanitizeForTTY pass. |
</threat_model>

<verification>
- `go build ./...` succeeds with the raw map consumed end-to-end.
- `go test ./cmd/... -run TestScanNoVerifyNoNetwork -count=1` green (no-network default, exit codes unchanged).
- `go test ./internal/output -run 'TestHumanVerificationTag|TestHumanVerifiedTally|TestVerifyOmittedByDefault' -count=1` green.
- `go test ./... -run TestHookOffline -count=1` green (hook stays offline).
- Phase gate: `go test -race ./... -count=1` fully green + `go vet ./...` clean.
</verification>

<success_criteria>
- `--verify` is registered off-by-default and invokes verify.Run on newFindings before output; without it, no network and byte-identical OUT-02 JSON.
- Human output shows the colored ACTIVE/INACTIVE/UNKNOWN tag and the Verified tally; non-verify rows are unchanged.
- The exit-code contract is untouched (label-only).
- The pre-commit hook template never contains --verify; the full -race suite + go vet are green.
</success_criteria>

<output>
Create `.planning/phases/04-opt-in-live-verification-aws-github/04-03-SUMMARY.md` when done.
</output>
