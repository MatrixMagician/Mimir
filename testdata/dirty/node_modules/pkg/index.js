// node_modules content carrying a fake GitHub OAuth token. Plan 03 proves the
// default path-prune skips node_modules/, so this never becomes a finding.
const ghOAuth = "gho_FakeGitHubOAuth123456789012345678901";
module.exports = { ghOAuth };
