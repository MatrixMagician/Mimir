---
phase: 02-false-positive-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/finding/finding.go
  - internal/scanner/scanner.go
  - internal/suppress/doc.go
  - testdata/dirty/
  - testdata/dirty-moved/
  - testdata/dirty-shifted/
  - internal/suppress/fixtures_test.go
autonomous: true
requirements: [SUP-01, SUP-02, SUP-03, SUP-04]
must_haves:
  truths:
    - "doublestar/v4 v4.10.0 is a direct dependency resolvable by the build"
    - "A dirty fixture repo exists exercising a real secret, noisy dirs, an inline-ignore line, a lockfile, and a *.min.js"
    - "Moved-file and blank-line-shifted variants of the dirty fixture exist for criterion-4 stability tests"
    - "Finding JSON carries optional suppressed / suppression_reason fields that are omitted when empty"
    - "Stats carries a paths-excluded count and a suppressed-by-reason map"
  artifacts:
    - path: "internal/suppress/doc.go"
      provides: "internal/suppress package declaration + Phase 4 decoupling note"
      contains: "package suppress"
    - path: "internal/finding/finding.go"
      provides: "Finding with Suppressed + SuppressionReason omitempty fields (D-12)"
      contains: "SuppressionReason"
    - path: "internal/scanner/scanner.go"
      provides: "Stats.PathsExcluded + Stats.Suppressed map (D-11/D-13)"
      contains: "PathsExcluded"
    - path: "testdata/dirty"
      provides: "dirty fixture repo root for Phase 2 integration + criterion-4 tests"
  key_links:
    - from: "internal/finding/finding.go"
      to: "encoding/json"
      via: "omitempty struct tags preserving OUT-02 schema"
      pattern: "suppressed,omitempty"
---

<objective>
Establish the Phase 2 foundation that every suppression slice builds on: promote
`doublestar/v4` to a direct dependency, create the `internal/suppress` package
shell, extend the `Finding` model with the two `--show-suppressed` fields (D-12),
extend `scanner.Stats` with the transparency counters (D-11/D-13), and build the
"dirty" fixture repo (plus its moved-file and blank-line-shifted variants) that
the criterion-4 stability tests and CLI integration tests in later plans depend on.

This is the contract-defining wave (interface-first): the structs and fixtures
land here so Plans 02–04 implement against fixed contracts instead of inventing them.

Purpose: Phase 2 adds no new detection — it plumbs four suppression layers into the
Phase 1 scanner. Those layers all need: the new dependency, a home package, the
extended data model, and a dirty repo to prove behavior. Landing them together
keeps the later vertical slices small and conflict-free.
Output: Updated go.mod/go.sum, internal/suppress/doc.go, extended Finding + Stats,
testdata/dirty + testdata/dirty-moved + testdata/dirty-shifted, a fixtures sanity test.
</objective>

<execution_context>
@/home/oliverh/repos/github/MatrixMagician/Mimir/.claude/get-shit-done/workflows/execute-plan.md
@/home/oliverh/repos/github/MatrixMagician/Mimir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-false-positive-control/02-CONTEXT.md
@.planning/phases/02-false-positive-control/02-RESEARCH.md
@.planning/phases/02-false-positive-control/02-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doublestar/v4 dependency and create the internal/suppress package shell</name>
  <read_first>
    - go.mod (current direct deps; doublestar is NOT yet listed — confirm)
    - CLAUDE.md "Recommended Stack" + Sources block (doublestar/v4 v4.10.0 pinned, MIT, verified on proxy.golang.org)
    - 02-RESEARCH.md §Installation + §Package Legitimacy Audit (Approved disposition) + §Environment Availability (module cache populated; non-interactive shell may report `go: command not found` — source profile or use absolute go path)
    - 02-PATTERNS.md §"Recommended Project Structure" (the internal/suppress layout) and §"No Analog Found"
  </read_first>
  <action>
    Promote `github.com/bmatcuk/doublestar/v4` to a direct dependency pinned to v4.10.0
    (the exact CLAUDE.md-mandated version — do NOT use a different version). Run
    `go get github.com/bmatcuk/doublestar/v4@v4.10.0` then `go mod tidy`. If the shell
    reports `go: command not found`, source the user profile or invoke `go` by absolute
    path (per 02-RESEARCH.md Environment Availability note). Confirm go.mod now lists
    doublestar/v4 in the direct `require` block and go.sum references it.
    Then create `internal/suppress/doc.go` containing only the package declaration
    (`package suppress`) and a package doc comment. The doc comment MUST state the
    package's responsibility (owns the three suppression mechanisms: inline-directive
    matcher, .mimirignore/default-glob path matcher, baseline OR-match filter per D-01..D-10)
    AND the load-bearing decoupling constraint from 02-RESEARCH.md anti-patterns: the
    baseline filter is a single post-`g.Wait()` stage, NOT run inside the per-file
    goroutine, so Phase 4 verification can slot into the same pipeline position.
    Do NOT add any other Go files in this task (inline.go, pathmatch.go, baseline.go
    are owned by Plans 02/03/04). Do NOT modify config/mimir.toml's existing
    `[[allowlists]] paths` block (Open Q2 — keep for now).
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Mimir && go build ./... && grep -q 'github.com/bmatcuk/doublestar/v4 v4.10.0' go.mod && go vet ./internal/suppress/</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` lists `github.com/bmatcuk/doublestar/v4 v4.10.0` as a direct require (not `// indirect`); ties to 02-RESEARCH.md Standard Stack.
    - `go build ./...` exits 0 with the new package present.
    - `internal/suppress/doc.go` declares `package suppress` and its doc comment names the post-`g.Wait()` decoupling constraint (Phase 4 slot-in).
    - No new `.go` files beyond `doc.go` added under `internal/suppress` in this task.
  </acceptance_criteria>
  <done>doublestar/v4 v4.10.0 is a direct dep, the build is green, and the empty internal/suppress package exists with the decoupling note documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Finding (D-12 fields) and Stats (D-11/D-13 counters)</name>
  <read_first>
    - internal/finding/finding.go:25-36 (the Finding struct + JSON tags) and finding.go:1-6 (the redact-at-boundary security invariant + reflect-test guard reference)
    - internal/finding/finding_test.go (the reflect-inspection raw-secret guard — it MUST still pass after the new fields)
    - internal/scanner/scanner.go:24-28 (current Stats struct) and scanner.go:55,115 (the existing `filesScanned atomic.Int64` pattern)
    - 02-PATTERNS.md §"internal/finding/finding.go (MOD)" and §"internal/scanner/scanner.go (MOD) — (3) Extend Stats"
    - 02-RESEARCH.md §"Code Examples" (exact field names + omitempty tags) and §D-11/D-12/D-13
  </read_first>
  <behavior>
    - Test: a Finding with no suppression marshals to JSON with NO `suppressed` and NO `suppression_reason` keys (omitempty preserves OUT-02 schema for consumers not passing --show-suppressed).
    - Test: a Finding with Suppressed=true, SuppressionReason="baseline" marshals to JSON containing both keys with those values.
    - Test: the existing finding_test.go reflect raw-secret guard still passes (no raw secret in any field; the two new fields carry no secret).
    - Test (scanner): a zero-value Stats has PathsExcluded==0 and a usable (non-nil after init) Suppressed map keyed by reason strings.
  </behavior>
  <action>
    In `internal/finding/finding.go`, add exactly two fields to the `Finding` struct per
    D-12: `Suppressed bool `json:"suppressed,omitempty"`` and
    `SuppressionReason string `json:"suppression_reason,omitempty"`` (reason values will be
    one of `baseline` | `inline-ignore` | `allowlist`). Both MUST use `omitempty` so the
    Phase 1 OUT-02 stable schema is byte-identical for findings that are not suppressed.
    Do NOT store any raw secret — these fields carry only the bool + reason string;
    the redact-at-boundary invariant (finding.go:3-6) is unchanged. `New()` does not set
    them (callers annotate later).
    In `internal/scanner/scanner.go`, extend `Stats` (scanner.go:24-28) with
    `PathsExcluded int` (D-13 aggregate count) and `Suppressed map[string]int` (reason→count,
    D-11). Do NOT yet wire population logic (that lands in Plans 02–04); just add the fields
    and ensure the existing `Scan` return at scanner.go:147 still compiles (leave the new
    fields at their zero values for now, or initialize `Suppressed: map[string]int{}`).
    Update the finding test file (or add a small test) to assert the omitempty marshal
    behavior described in <behavior>.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Mimir && go test ./internal/finding/... ./internal/scanner/... -race</automated>
  </verify>
  <acceptance_criteria>
    - `internal/finding/finding.go` Finding struct contains `Suppressed` and `SuppressionReason` with `omitempty` JSON tags (source assertion).
    - `go test ./internal/finding/... -race` passes including the unchanged reflect raw-secret guard and a new omitempty-marshal assertion (behavior: unsuppressed finding JSON has neither key).
    - `internal/scanner/scanner.go` Stats has `PathsExcluded int` and `Suppressed map[string]int`; `go test ./internal/scanner/... -race` passes.
    - JSON for a finding with `suppressed,omitempty` absent matches the exact Phase 1 OUT-02 schema (no extra keys).
  </acceptance_criteria>
  <done>Finding has the two omitempty suppression fields, Stats has the two transparency counters, all finding + scanner tests (incl. the raw-secret guard) pass.</done>
</task>

<task type="auto">
  <name>Task 3: Build the dirty fixture repo + criterion-4 variants + fixtures sanity test</name>
  <read_first>
    - testdata/fixtures/known-secrets.txt (REUSE these exact Phase 1 fixture-secret values — do not invent new secrets)
    - testdata/clean/no-secrets.go (the clean-fixture shape)
    - cmd/mimir/scan_test.go:1-64 (TestMain builds the binary; runMimir runs `runMimir(t, "scan", "--no-color", "testdata/clean/")` with cwd = repoRoot — fixtures are addressed repo-relative)
    - 02-RESEARCH.md §"Wave 0 Gaps" (the dirty-fixture requirement list) and §"Criterion-4 stability validation"
    - MEMORY: executor-semgrep-fixture-trap — there is NO semgrep commit-block hook; do NOT add `.semgrepignore` commit blocks or fabricate hooks. Honor existing `.semgrepignore` rules only.
  </read_first>
  <action>
    Create `testdata/dirty/` — a fixture repo root that exercises every Phase 2 layer:
    (1) a file containing a real Phase-1 fixture secret at a known path (e.g.
    `testdata/dirty/src/app.go` with an aws-access-token value reused verbatim from
    testdata/fixtures/known-secrets.txt) so it produces a deterministic finding +
    fingerprint;
    (2) a noisy `testdata/dirty/vendor/` dir and a `testdata/dirty/node_modules/` dir each
    containing a file with a fixture secret (to prove default path-prune in Plan 03);
    (3) a `testdata/dirty/app.min.js` containing a fixture secret (default *.min.js prune);
    (4) a lockfile e.g. `testdata/dirty/package-lock.json` containing a fixture secret;
    (5) a file `testdata/dirty/ignored.go` with a secret on a line that also contains the
    inline directive `mimir:ignore` (blanket) AND a second file/line pairing two rules with
    `mimir:ignore:<rule-id>` scoping ONE rule (for Plan 02 scoped-vs-blanket tests);
    (6) a `testdata/dirty/.mimirignore` containing at least one `**` glob and one `!negation`
    line (for Plan 03);
    (7) a checked-in baseline `testdata/dirty/.mimir-baseline.json` is NOT created here
    (Plan 04 generates it via --baseline-out so the schema stays single-sourced).
    Then create `testdata/dirty-moved/` = a copy of `testdata/dirty/` with the secret-bearing
    `src/app.go` relocated to a different path (e.g. `lib/app.go`) — same secret value, same
    line content — to exercise the criterion-4 file-move OR-match.
    Then create `testdata/dirty-shifted/` = a copy with one or more blank lines inserted ABOVE
    the secret line in `src/app.go` (line number changes; secret value unchanged) — exercises
    criterion-4 blank-line-insert stability.
    Add `internal/suppress/fixtures_test.go` (package suppress, build-tag-free) with a single
    sanity test that asserts the three fixture dirs exist and the secret-bearing files contain
    the expected fixture-secret substring — proving the fixtures are wired before later plans
    consume them. Do NOT assert scanner behavior here (that belongs to Plans 02–04).
    Reuse the existing fixture-secret values exactly; do not fabricate new credentials. Add no
    semgrep/hook artifacts.
  </action>
  <verify>
    <automated>cd /home/oliverh/repos/github/MatrixMagician/Mimir && test -f testdata/dirty/.mimirignore && test -d testdata/dirty/vendor && test -f testdata/dirty-moved/lib/app.go && test -f testdata/dirty-shifted/src/app.go && go test ./internal/suppress/... -race</automated>
  </verify>
  <acceptance_criteria>
    - `testdata/dirty/` contains: a secret-bearing source file, `vendor/`, `node_modules/`, `*.min.js`, a lockfile, an inline-`mimir:ignore` file, and a `.mimirignore` with a `**` glob + a `!negation` (filesystem assertion).
    - `testdata/dirty-moved/` is the dirty fixture with the secret file at a DIFFERENT path, same secret value (criterion-4 file-move source).
    - `testdata/dirty-shifted/` is the dirty fixture with blank lines inserted above the secret line (criterion-4 blank-line source).
    - All secret values are reused verbatim from `testdata/fixtures/known-secrets.txt`; no fabricated secrets; no semgrep/hook files added.
    - `go test ./internal/suppress/... -race` passes (fixtures sanity test green).
  </acceptance_criteria>
  <done>The dirty fixture repo and its two criterion-4 transform variants exist with reused fixture secrets, and the suppress-package fixtures sanity test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content → Finding model | Untrusted file bytes flow into Finding; the new omitempty fields must carry no raw secret |
| test fixtures → committed repo | Fixture secrets are intentionally fake (reused Phase 1 values) but must never be REAL credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Finding.Suppressed / SuppressionReason fields | mitigate | New fields are bool + enum-string only; redact-at-boundary invariant (finding.go:3-6) unchanged; reflect raw-secret guard in finding_test.go must still pass (Task 2 acceptance) |
| T-02-02 | Information Disclosure | testdata/dirty fixtures | mitigate | Reuse only the existing fake Phase-1 fixture secrets (known-secrets.txt); never introduce a real credential into testdata |
| T-02-SC | Tampering | go get doublestar/v4 install | mitigate | Pinned to CLAUDE.md-verified v4.10.0; 02-RESEARCH.md Package Legitimacy Audit disposition = Approved (MIT, in module cache, long-established). Go build model has no postinstall risk. Not [ASSUMED]/[SUS] → no blocking checkpoint required |
</threat_model>

<verification>
- `go build ./...` green with doublestar/v4 v4.10.0 direct dep.
- `go test ./internal/finding/... ./internal/scanner/... ./internal/suppress/... -race` all green.
- Reflect raw-secret guard in finding_test.go still passes after the two new fields.
- Three fixture dirs (`dirty`, `dirty-moved`, `dirty-shifted`) present with reused fixture secrets.
</verification>

<success_criteria>
- doublestar/v4 v4.10.0 is a direct module dependency.
- `Finding` exposes `Suppressed` + `SuppressionReason` (both omitempty); OUT-02 schema preserved for unsuppressed findings.
- `Stats` exposes `PathsExcluded` + `Suppressed map[string]int`.
- The dirty fixture repo and its moved + shifted criterion-4 variants exist and are sanity-tested.
- No raw secret stored anywhere; no fabricated credentials in testdata.
</success_criteria>

<output>
Create `.planning/phases/02-false-positive-control/02-01-SUMMARY.md` when done.
</output>
